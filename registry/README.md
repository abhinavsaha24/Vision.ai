# Registry

This directory stores versioned edge registry snapshots generated by the research pipeline.

- Generated artifacts should be written under `registry/versions/`.
- Runtime should consume a selected version through `EDGE_REGISTRY_PATH`.
- Keep only metadata/docs in source control; generated JSON snapshots are ignored.
